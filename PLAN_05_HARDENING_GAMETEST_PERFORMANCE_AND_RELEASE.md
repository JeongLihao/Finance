# 长任务计划五：兼容加固、GameTest、性能审计与发布验收

> 预计执行时间：12～24 小时，可拆成多个长会话。
>
> 前置条件：计划一至四功能完成。
>
> 本计划不再扩大核心玩法范围，目标是把新增系统做到可在多人服务器长期运行、可升级、可诊断、可发布。

---

## 1. 最终目标

把 Minecraft-first 改造成一个可靠版本：

- 旧 0.4.1 存档升级不丢任何资产。
- 新仓库、合同、公司设施和反馈跨重启稳定。
- 恶意/异常数据包不能绕过权限或复制资产。
- 世界切换不泄漏静态状态。
- 长期模拟确定且资产守恒。
- 关键方块交互有 GameTest。
- 专用服务器和客户端均可启动。
- 文档、版本、变更日志和回退策略齐全。

---

## 2. 范围冻结

本计划开始后默认冻结新功能。只允许：

- 修复缺陷。
- 补测试。
- 优化性能。
- 完善迁移、诊断、权限和文档。
- 修复必要 UX 阻塞。

禁止顺手增加新的金融产品、新机器链、新 NPC AI 或大规模视觉重制。

---

## 3. 基线快照

记录：

- git status 和用户改动。
- Java/Forge/Gradle 版本。
- 测试套件和测试总数。
- `EconomySavedData.currentDataVersion()`。
- 生产 Java 文件数量。
- 当前构建时间。
- 标准 365 日和 1000 日模拟耗时。

执行：

```powershell
.\gradlew.bat cleanTest test
.\gradlew.bat compileJava
git diff --check
```

---

## 4. 任务 A：全资产清单和守恒模型

列出所有资产端点：

- 玩家可用/冻结账户。
- NPC 市场账户和库存。
- 央行储备。
- 公司现金、legacy inventory、custody inventory。
- 银行准备金、存款、贷款和权益。
- 债券/票据/基金/期货/保险托管。
- 仓库托管商品。
- 合同 escrow。
- 公司行动 escrow。

在 `docs/ASSET_CONSERVATION_MAP.md` 画出每类操作的借方/贷方或来源/去向。

检查新增代码是否存在直接 deposit 造币、直接 addCommodity 造物、异常补偿不对称。

为每个跨端点操作列出：预检、提交顺序、回滚、幂等 key、交易记录。

---

## 5. 任务 B：存档版本与迁移矩阵

审查所有序列化器：

- 根 key 唯一。
- 集合有保存上限和加载上限。
- UUID/枚举/日期/金额验证。
- 坏记录隔离。
- 加载顺序正确。
- resetRuntimeState 覆盖新静态管理器。

扩展 `MigrationMatrixTest`：

- 历史版本 13、15、17、19～当前。
- 没有 gameplay 数据的旧存档。
- 有旧虚拟商品但无仓库。
- 有旧公司但无经营模式。
- 新版本保存后重复加载。
- 新字段损坏但金融内核有效。

构建组合损坏 fixture：

- 一个坏仓库、一个好仓库。
- 一个坏合同、一个好合同。
- 设施引用缺失公司。
- escrow 不足。
- 重复 operation key。

目标：可恢复记录保留，无法安全恢复的记录隔离并诊断，不整库崩溃。

---

## 6. 任务 C：世界切换和生命周期

审查：

- `FinanceMod.onServerStarting`
- `onServerStopped`
- `EconomySavedData.resetRuntimeState`
- `unload`
- 客户端 logout cache clear

新增测试：

- 世界 A 创建仓库/合同/公司设施。
- unload/reset。
- 世界 B 空存档不继承任何 A 状态。
- 再加载 A 能恢复。

清理：

- 仓库索引。
- 合同索引。
- 成员邀请。
- 终端会话。
- 世界反馈冷却。
- 客户端新缓存。

不清理模组默认商品注册定义。

---

## 7. 任务 D：网络安全审计

逐个检查所有新旧 C2S 包：

- sender null。
- 权限。
- 模块健康。
- 请求频率。
- 字符串上限。
- 数值正数/范围/溢出。
- 枚举解码。
- UUID 所有权。
- 终端位置、维度和距离。
- 当前菜单会话。
- operation key。

逐个检查 S2C：

- 集合分配前上限。
- 私有数据裁剪。
- requestId。
- 字符串上限。

增加 fuzz 风格参数化测试：边界值、Long.MAX_VALUE、负数、超长 UTF-8、非法 ordinal、巨大 list count。

不能依赖 GUI 按钮限制作为安全边界。

---

## 8. 任务 E：GameTest 基础设施

在 Forge 1.20.1 GameTest 体系中增加测试注册。先确保 headless GameTest server 可启动。

建议测试：

### 终端

- 方块放置和菜单入口。
- 走远后失效。
- 破坏后操作拒绝。
- 普通玩家不能打开央行控制台。

### 仓库

- 首次创建唯一 ID。
- 铁锭存入守恒。
- 提取守恒。
- 背包满拒绝。
- 两次重复请求幂等。
- 拆除后资产不丢失。

### 合同

- 交付物品和 escrow 支付。
- 重复交付不二次奖励。
- 远程交付拒绝。

### 公司设施

- 绑定仓库。
- 原料消费和产出。
- 同日不重复。
- 风险状态暂停。

GameTest 失败必须给出清晰断言，不依赖随机时序。

---

## 9. 任务 F：并发和重复交互

虽然 Minecraft 主线程串行，快速数据包、漏斗和菜单仍可形成读后写问题。

审查：

- 同一仓库两玩家同时提取。
- 两人竞争接受合同。
- 合同交付与过期日结同 tick。
- 公司设施生产与玩家提取同 tick。
- 方块拆除与菜单操作。
- 保存发生在状态转换边界。

使用服务端线程串行化、synchronized 边界或明确事务锁。不要引入复杂异步金融结算。

测试操作顺序的所有可重复排列，确保不会负数或重复支付。

---

## 10. 任务 G：性能剖析

检查热点：

- `FinancialCycleService.advanceTo` 时间跳跃。
- 合同每日生成和过期。
- 仓库容量统计。
- 公司设施日结。
- 世界反馈受众选择。
- 菜单数据构建。
- `EconomySavedData.save/load`。

建立基准场景：

- 1000 玩家账户。
- 500 仓库。
- 1000 合同历史、100 活跃合同。
- 100 公司、200 设施。
- 现有最大订单和行情历史的合理比例。

目标：

- 不出现 O(玩家 × 仓库 × 商品) 每 tick 扫描。
- 菜单只构建相关玩家/公司数据。
- 日结工作有上限。
- 保存集合有界。

优化前后记录耗时，不做无证据微优化。

---

## 11. 任务 H：长期模拟

扩展标准模拟：

- 365 日固定 seed。
- 每 30 日保存/加载。
- 钱包、银行、仓库、合同、公司生产、基金等共同运行。
- 确定性 fingerprint。

压力模拟：

- 1000 日。
- 市场短缺、公司风险、银行流动性、期货结算和合同过期。
- 保持诊断无 ERROR/FATAL。

新增守恒指标：

- 合同 escrow 总额。
- 托管商品总量变化与模拟输入/输出。
- 公司生产消费/产出。
- 重启前后 fingerprint。

如随机噪音使生产引擎不确定，隔离非确定来源或注入 seed，不能简单删除确定性断言。

---

## 12. 任务 I：诊断与模块降级

扩展模块健康：

- WAREHOUSE
- CONTRACT（若现有枚举结构允许安全增加）
- COMPANY_GAMEPLAY

定义降级：

- 仓库异常：禁止新存入，允许安全提取。
- 合同异常：禁止新发布/接受，允许可验证退款。
- 公司设施异常：暂停新生产，不影响旧金融合同履约。

启动自检继续分 tick 执行，不让新检查一次卡住世界加载。

诊断必须只报告/降级，不自动猜测修复资产。

---

## 13. 任务 J：资源和专用服务器审计

检查：

- 所有注册方块 item model。
- blockstate 完整。
- loot table。
- recipe。
- zh_cn/en_us key。
- advancement JSON。
- 不存在未引用/拼错资源。

启动专用服务器，确保：

- 客户端 Screen、Minecraft.getInstance 不在公共注册路径加载。
- BlockEntity/Menu 注册正确。
- 网络协议版本兼容策略明确。

启动客户端，检查所有终端模型和界面。

---

## 14. 任务 K：UX 错误路径

逐条验证玩家可理解的失败信息：

- 权限不足。
- 距离过远。
- 仓库超容量。
- 背包空间不足。
- 商品不可实体化。
- 资产已被订单锁定。
- 合同过期。
- escrow 不足。
- 公司设施缺料/满仓。
- 银行限制业务。
- 模块因诊断暂停。

使用翻译 key，不向普通玩家显示堆栈或内部 UUID（必要短 ID 除外）。

---

## 15. 任务 L：回退与兼容模式

验证 `minecraftFirstMode=false`：

- 旧完整界面仍能使用。
- 新数据仍加载和结算。
- 不要求世界终端才能兑付已有资产。

验证服务器关闭新玩法入口后：

- 玩家仍能取消订单和领取到期资产。
- 仓库资产可通过管理员/兼容提取路径恢复。
- 合同不无限卡死，存在退款/终止流程。

在 `docs/ROLLBACK_AND_RECOVERY.md` 写管理员恢复手册。

---

## 16. 任务 M：版本和发布文档

更新：

- README：Minecraft-first 主循环置顶，高级金融作为深度层。
- CHANGELOG：新增内容、兼容说明、管理员配置。
- VERSION：按项目版本策略。
- DEVELOPMENT_NOTES：新包、存档顺序、测试。
- 服务器迁移指南。
- 玩家快速上手。
- 管理员故障恢复。

不要宣称未验证的兼容性。

---

## 17. 任务 N：最终构建与制品检查

执行：

```powershell
.\gradlew.bat cleanTest test
.\gradlew.bat build
git diff --check
```

检查 JAR：

- mods.toml 展开正确。
- 资源存在。
- 无测试类混入。
- 版本正确。
- 文件大小合理。

不要发布或上传，除非用户明确授权。

---

## 18. 检查点

1. 资产守恒图与代码审计。
2. 迁移矩阵和生命周期。
3. 网络 fuzz 与权限审计。
4. GameTest。
5. 并发/日结顺序。
6. 性能和长期模拟。
7. 诊断降级。
8. 资源、专服、客户端。
9. 文档、版本和最终构建。

每个检查点更新 `docs/MINECRAFTIZATION_PROGRESS.md`，包含测试结果和未解决风险。

---

## 19. 发布阻断条件

出现以下任一项不得判定完成：

- 资产复制或丢失。
- 旧存档加载失败。
- 世界切换继承旧状态。
- 普通玩家绕过管理员权限。
- 重复请求二次奖励。
- 专用服务器类加载崩溃。
- 1000 日模拟非确定或出现 ERROR/FATAL。
- GameTest 关键仓库/合同流程失败。
- 缺模型、缺翻译或无效 recipe/advancement。
- 完整测试有失败或跳过而未解释。

---

## 20. 最终验收场景

在全新世界：

1. 获得账本并查看钱包。
2. 制作市场终端和仓库。
3. 采集、存仓、出售、完成合同。
4. 创建公司、邀请成员、建设设施、生产销售。
5. 使用银行柜台。
6. 公司上市后使用证券终端。
7. 管理员使用央行控制台。
8. 经历至少一次市场短缺反馈。
9. 重启服务器并继续所有流程。

在旧存档：

1. 所有账户、订单、库存、股票、债券、基金、期货和保险保持。
2. 旧商品在无仓库时可安全访问。
3. 旧公司继续 LEGACY 经营。
4. 放置新设施后可选择迁移到 HYBRID。
5. 重复加载不增发资产。

---

## 21. 完成标准

- 全资产守恒和迁移测试完整。
- 新旧世界生命周期隔离。
- 网络和权限审计无已知高风险缺口。
- GameTest 覆盖终端、仓库、合同、公司生产。
- 365/1000 日模拟健康且确定。
- 性能没有无界 tick 扫描。
- 专服和客户端冒烟通过。
- 完整测试和 build 通过。
- 文档、恢复路径和版本信息齐全。

---

## 22. 最终交付报告

执行窗口必须输出：

- 最终玩家主循环。
- 资产守恒审计结论。
- 数据版本和迁移矩阵。
- 网络安全矩阵。
- GameTest/单测/长期模拟结果。
- 性能基线与优化结果。
- 专服和客户端验证结果。
- 未解决风险。
- 构建制品路径。
- 是否达到发布标准；若否，列出阻断项。

