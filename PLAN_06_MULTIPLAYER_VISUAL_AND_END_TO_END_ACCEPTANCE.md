# 长任务计划六：真人多人、视觉与端到端验收闭环

> 预计执行时间：10～18 小时。若发现阻断缺陷，可延长到 24 小时。
>
> 前置条件：计划一至五已经合入，当前全量单测、GameTest 和 build 通过。
>
> 核心原则：本计划以验收和修复为主，不扩展金融产品，不删除旧系统。

---

## 1. 最终目标

消除当前唯一明确未完成项：真实 Forge 客户端与多人服务器中的可玩性证据。最终必须证明玩家无需打开超级金融 GUI，也能在世界中完成：

1. 获得账本并查看钱包。
2. 制作并放置市场终端、仓库控制器。
3. 将真实物品存入仓库并出售。
4. 接受并完成一个有真实 escrow 的合同。
5. 创建公司、邀请第二名玩家、绑定仓库和设施。
6. 经过一个 MC 日完成真实生产并销售产物。
7. 使用银行、证券、董事会和管理员入口。
8. 重启专用服务器后继续同一流程。

完成后，`docs/MINECRAFTIZATION_PROGRESS.md` 不再把“真人多人客户端视觉验收”列为开放项。

---

## 2. 开始前基线

- 记录 `git status --short`，不得覆盖用户已有改动。
- 记录 Java、Forge、Gradle、模组版本、数据版本和协议版本。
- 执行 `cleanTest test`、`runGameTestServer`、`build`。
- 保存当前测试数量、GameTest 数量和 JAR 哈希。
- 创建新的临时验收世界，不复用开发世界作为唯一证据。
- 准备两个独立玩家身份：普通玩家 A、普通玩家 B；另准备管理员身份。

如果无法真正启动两个客户端，不能宣称本计划完成；必须把阻断原因写入交接报告。

---

## 3. 任务 A：专用服务器启动与类加载审计

1. 使用 Forge 专用服务器运行配置，而非只依赖 GameTest 服务器。
2. 检查公共注册、事件、菜单和网络代码没有直接加载客户端类。
3. 检查服务器日志中的 missing registry、missing model、数据包解码、SavedData 和 mixin 异常。
4. 玩家 A、B 依次加入、退出、重连，确认静态缓存不会跨玩家泄漏。
5. 切换世界或重启服务器，确认运行时状态先清理再恢复。
6. 记录启动耗时、停服保存耗时和重启恢复耗时。

不得通过删除失败内容、清空存档或关闭模块来伪造启动成功。

---

## 4. 任务 B：入口和视觉逐项验收

逐一放置并交互：

- 市场终端。
- 仓库控制器。
- 银行柜台。
- 公司办公桌。
- 工厂控制器。
- 证券终端。
- 董事会桌。
- 央行控制台。

每个入口检查：

- 方块、物品、名称、tooltip、破坏粒子和掉落是否正确。
- 方块模型是否缺失、紫黑、穿模或朝向错误。
- 菜单尺寸在 100%、GUI Scale 2、3、4 下不越界。
- 低分辨率窗口下按钮、滚动区和文字仍可操作。
- 状态灯、粒子、音效、toast 不重复刷屏。
- 走远、跨维度、方块被破坏后，菜单立即失效。
- 普通玩家不能从央行或管理员页面读取私有数据。
- Esc 关闭、重新打开、死亡和重连不会留下幽灵菜单。

将发现的问题按 P0～P3 分类，只修复与既定玩法、可靠性和可访问性相关的问题。

---

## 5. 任务 C：双人仓库与市场闭环

按固定脚本执行并记录每一步前后资产：

1. A 放置仓库并存入一组铁锭。
2. B 尝试提取，服务端必须拒绝且资产不变。
3. A 授权或保持私有权限，验证权限变更立即生效。
4. A 从仓库挂出部分铁锭，锁定数量不能再提取。
5. B 购买，验证双方现金、仓库托管、冻结资产和成交记录。
6. A 快速双击存入、提取、出售按钮，验证操作键幂等。
7. 在背包近满、仓库近满、余额近上限条件下重复测试。
8. 破坏仓库方块，确认托管资产不消失且记录进入可恢复状态。
9. 原位置重建仓库，验证身份恢复策略符合文档。

为存入、提取、市场出售和拆除保全至少各补一个 GameTest 或等价的服务端集成测试。现有只测注册的仓库 GameTest 不足以替代真实物品移动验证。

---

## 6. 任务 D：合同交付端到端

1. 生成一个预算充足的 NPC 采购合同。
2. 确认奖励先从发行方进入 escrow，而不是完成时凭空生成。
3. A 接受合同并绑定有权限的仓库。
4. 数量不足时交付失败，物品和 escrow 不变。
5. 数量充足时，真实物品减少、奖励到账、escrow 归零、合同完成。
6. 重复点击、重复数据包、重连后重复请求均不能二次领奖。
7. 合同过期时只退款一次。
8. 发行方、仓库或商品记录损坏时隔离坏记录，不移动资产。

新增 GameTest 覆盖“背包/仓库物品 → 合同目的托管 → escrow 奖励”的完整成功路径，而不只测试接受合同。

---

## 7. 任务 E：双人公司生产闭环

1. A 创建公司和办公桌。
2. A 邀请 B 为仓库员工或生产经理。
3. B 绑定公司仓库和工厂控制器。
4. 未授权角色尝试升级、解绑、提取和改自动销售策略，必须拒绝。
5. 公司发布采购合同并由成员交付原料。
6. 等待自然 MC 日切换，不用命令直接调用生产方法作为唯一验证。
7. 设施消费托管原料、扣维护费、产生商品并更新状态灯。
8. 公司出售产物，现金、交易量和财报一致。
9. 同一日重开菜单、重连和重启，不能双产。
10. 进入破产风险后设施暂停，恢复后仅从下一合法周期继续。

补一个成功生产 GameTest，验证输入减少、输出增加、现金扣除和 `lastProcessedDay` 同时提交。

---

## 8. 任务 F：旧存档和迁移冒烟

- 备份一份真实旧版本存档，仅对副本测试。
- 加载后核对账户、订单、商品库存、公司、股票、债券、基金、期货和保险。
- 旧公司默认保持 LEGACY，不强迫迁移。
- 放置新仓库和设施后可显式切到 HYBRID。
- 连续加载同一存档三次，资产总量和数据版本不继续变化。
- 回退旧版本前验证文档说明，不直接用旧版本写入已升级存档。
- 故意损坏一个新增子记录，验证隔离而非拖垮整个经济存档。

---

## 9. 任务 G：缺陷修复纪律

每个缺陷先写最小复现：

- 服务端逻辑缺陷优先补单测。
- 方块生命周期缺陷优先补 GameTest。
- GUI 布局缺陷记录窗口尺寸和 GUI Scale。
- 资产问题记录操作前后所有端点。
- 网络问题记录玩家权限、菜单会话、维度、位置和操作键。

修复时禁止：

- 客户端直接决定奖励、余额、公司角色或升级成本。
- 通过扩大管理员权限掩盖普通流程问题。
- 捕获异常后静默吞掉资产不一致。
- 删除旧金融代码或旧存档兼容路径。
- 为通过测试而降低核心校验。

---

## 10. 最终回归与交付

必须执行：

```powershell
.\gradlew.bat cleanTest test
.\gradlew.bat runGameTestServer
.\gradlew.bat build
git diff --check
```

最终交接必须包含：

- 两名玩家、管理员、全新世界和旧存档的测试结果。
- 每个实体入口的视觉/交互结论。
- 仓库、合同、公司闭环的资产前后表。
- 新增测试名称和覆盖边界。
- 专服日志中仍存在的警告及影响判断。
- 修复过的缺陷清单。
- 未解决风险。
- 构建制品路径和哈希。

## 11. 完成标准

- 双人完整流程真实执行成功。
- 视觉、声音、粒子、状态灯和 toast 已目视验收。
- 专用服务器、图形客户端、GameTest、JUnit 和 build 全部通过。
- 合同完成与成功生产已有世界级集成覆盖。
- 重启、重连、重复点击不复制资产。
- 旧存档升级不增发、不丢失资产。
- 进度文档不再有“等待真人多人验收”的开放项。

