# Finance 模组“回到 Minecraft”超长实施计划

> 交接用途：本文件用于交给另一个 Codex 窗口持续执行。
>
> 项目目录：`D:\MCMOD\Finance`
>
> 当前基线：Minecraft 1.20.1、Forge 47.4.16、Java 17、模组版本 0.4.1；现有金融内核约 294 个生产 Java 文件，64 个测试文件，当前完整测试为 304 项且全部通过。

---

## 一、任务总目标

在不删除现有金融功能、不破坏旧存档、不牺牲现有金融系统深度的前提下，把 Finance 从“运行在 Minecraft 中的超级金融模拟器”改造成“以 Minecraft 生存、采集、建造、运输、仓储和玩家协作为主要体验，金融系统作为后台经济引擎”的模组。

最终玩家的主要循环应当是：

```text
发现订单或市场机会
        ↓
进入世界采集、种植、加工或采购真实物品
        ↓
把物品运输到交易所仓库或公司仓库
        ↓
通过世界中的终端、柜台或公告板完成交易/交付
        ↓
获得资金、公司收益、声望或设施升级
        ↓
建设更大的仓库、公司基地和生产网络
        ↓
解锁股票、债券、银行、基金、期货等高级金融玩法
```

金融模型继续负责价格形成、资产冻结、撮合、结算、分红、风险、持久化和审计；玩家默认看到的必须是 Minecraft 行为，而不是一组彼此孤立的报表和数字按钮。

---

## 二、不可违反的原则

### 2.1 不删除现有系统

以下模块必须保留，不能为了简化体验直接删除源码、存档字段或管理器：

- 玩家账户与交易记录
- 商品注册、商品托管库存、P2P 撮合、NPC 市场
- 公司、公司财报、融资、股票、条件委托
- 商业银行、复式总账、同业拆借、存款保险、央行流动性
- 公司债券、央行票据、贷款、信用评级、收益率估值
- 基金、定投、赎回排队
- 商品期货、保证金、每日盯市、强平和保障基金
- 保险、保单、理赔和风险事件
- 股东提案、回购、要约收购、公司重组
- 指数、K 线、MACD、RSI、订单簿、成交记录和排行
- 经济指标、一致性诊断、模块暂停和长期模拟

它们可以被移动到高级入口、通过进度解锁、默认折叠或配置隐藏，但不能被无迁移地移除。

### 2.2 不让旧存档丢失资产

- 不得重命名已有 NBT key，除非同时提供向后兼容读取。
- `EconomySavedData.DATA_VERSION` 只能在真正发生格式变化时增加。
- 新数据字段必须有安全默认值。
- 旧存档没有新世界交互数据时，应自动进入兼容状态，不能删除账户、持仓、冻结资产、订单、保单或合同。
- 新增仓库方块后，现有 `CommodityInventoryManager` 中的虚拟商品应继续有效，并被解释为“历史托管账户”，而不是强制清零。
- 加载损坏的单条新记录时，应隔离坏记录，不能使整个经济存档加载失败。

### 2.3 保持服务器权威

- 所有资金、物品、仓储、订单和任务结算必须在服务端完成。
- 客户端数据包只能表达意图，不能直接提交余额、奖励、成交结果或任务完成状态。
- 必须继续使用并扩展 `NetworkValidation`、`MarketDataRequestLimiter` 和模块健康状态检查。
- 任何“物品扣除 + 金融入账”的跨系统操作都必须预检、提交、失败补偿，不能出现扣了物品但没付钱或付了钱但物品仍在的状态。

### 2.4 Minecraft 行为优先

新增或改造的每个玩家功能都必须回答：

1. 玩家在世界中做了什么？
2. 是否涉及真实物品、位置、方块、运输、生产、建造或合作？
3. 完成后世界状态或玩家能力发生了什么变化？

如果一个功能只要求玩家打开 GUI、输入数字、等待日期推进，则只能归入高级金融层，不得成为默认主循环。

### 2.5 小步提交，始终可运行

- 每个阶段完成后都必须能编译并通过测试。
- 不进行一次性大重写。
- 不在同一个提交里同时重构金融内核和新增玩法。
- 优先增加适配层，不直接侵入已稳定的管理器。
- 不修改与当前阶段无关的用户改动。

---

## 三、现有架构判断

### 3.1 必须保护的稳定内核

以下类是现有经济状态和结算的核心，应尽量少改：

- `finance.account.AccountManager`
- `finance.commodity.CommodityInventoryManager`
- `finance.market.MarketManager`
- `finance.market.NpcMarketMaker`
- `finance.company.CompanyManager`
- `finance.stock.StockOrderManager`
- `finance.bank.BankingManager`
- `finance.debt.CorporateBondManager`
- `finance.debt.CompanyLoanManager`
- `finance.fund.FundManager`
- `finance.futures.FuturesMarketManager`
- `finance.insurance.InsuranceManager`
- `finance.governance.CorporateActionManager`
- `finance.cycle.FinancialCycleService`
- `finance.data.EconomySavedData`

改造时应在它们外部增加 Minecraft 玩法适配层。只有发现明确缺少原子 API 时，才为内核增加小型、可测试的服务方法。

### 3.2 当前主要问题

- `FinanceScreen` 超过 3,200 行，几乎承载了全部玩家操作。
- 复杂金融分页在玩家第一次打开模组时就全部暴露。
- 商品托管库存虽然与真实背包可交换，但缺少世界中的仓库实体和空间感。
- 公司每日经营主要发生在服务端数字模拟中，玩家不需要参与生产或运输。
- 金融产品很完整，但没有足够的 Minecraft 动机解释玩家为什么要使用它们。
- 命令、GUI 和网络包存在重复入口，容易让业务规则漂移。
- 世界反馈偏少：市场短缺、公司风险、重大事件主要以数字或文本呈现。

### 3.3 新增中间层

建议新增 `finance.gameplay` 包，作为 Minecraft 世界与金融内核之间的唯一主要桥梁：

```text
方块 / 物品 / NPC / 菜单 / 世界事件
                   ↓
          finance.gameplay
                   ↓
账户 / 商品 / 市场 / 公司 / 银行等现有管理器
                   ↓
          EconomySavedData
```

初期可包含：

- `FinanceAccessContext`：玩家、维度、位置、入口类型、权限。
- `FinanceTerminalType`：钱包、市场、仓库、银行、公司、证券、董事会、央行。
- `FinanceGameplayService`：统一验证入口、调用现有管理器、返回结果。
- `GameplayActionResult`：成功、失败、消息、可选音效/粒子/刷新标记。
- `GameplayProgressService`：控制普通/高级功能解锁。
- `WarehouseService`：真实物品与托管库存之间的原子桥梁。
- `ContractManager`：世界任务与金融奖励的状态管理。
- `WorldEconomyFeedbackService`：把经济事件反馈到世界和玩家。

不要把新的所有逻辑再次堆进一个巨型类。服务之间按职责拆分，并给纯逻辑部分写 JUnit 测试。

---

## 四、目标体验分层

### 4.1 随身钱包层

任何玩家可使用，功能极简：

- 当前可用余额和冻结余额
- 向附近或指定玩家转账
- 最近 10 条与自己相关的交易
- 当前正在追踪的任务
- 指向最近市场终端或银行柜台的提示

建议载体：可合成的“金融账本”物品，右键打开轻量界面。不要让按键默认打开完整 `FinanceScreen`。

### 4.2 市场与仓库层

通过世界方块访问：

- 市场终端：价格、简单买卖、挂单、领取收购合同
- 仓库控制器：存入/取出真实商品、容量、权限和托管状态
- 公告板：本地任务、短缺商品、公司招聘、运输订单

普通界面只显示：名称、当前价格、涨跌、库存、买卖按钮。K 线和技术指标放入高级证券终端。

### 4.3 公司经营层

公司不再只是每日自动产生数字收益，而应包含：

- 公司办公室或办公桌方块
- 公司共享仓库
- 成员与权限
- 原材料采购任务
- 产品交付任务
- 设施升级
- 简单收入、成本、现金和风险概览

现有自动生产可以保留作为兼容/兜底机制，但新公司默认应选择“玩家参与模式”；服务器配置可允许旧式全自动模式。

### 4.4 高级金融层

使用“证券交易终端”打开现有完整金融体验：

- 股票、债券、基金、期货
- K 线、技术指标、订单簿、排行
- 信用评级、收益率曲线
- 保险产品

该层不需要删减金融深度，只需要改变默认入口和信息层级。

### 4.5 管理和监管层

通过仅管理员可用的“央行控制台”访问：

- 货币政策
- 系统银行完整资产负债表
- 压力测试
- 模块健康状态
- 经济指标
- 商品注册和价格历史清理

普通玩家只能看到经过裁剪的公开信息。

---

## 五、建议实施阶段总览

不要并行大规模实施多个阶段。推荐顺序如下：

1. 基线保护与开发约定
2. 玩法配置与入口类型
3. 简洁主界面和高级终端分流
4. 方块、物品和菜单注册骨架
5. 市场终端
6. 仓库控制器与真实物品托管
7. 合同/任务系统
8. 公司世界化
9. 世界经济反馈
10. 银行与高级金融终端世界化
11. 进度、配方、教程和可发现性
12. 兼容、迁移、安全和性能加固
13. 综合测试与长期模拟扩展
14. 文档、版本和发布准备

每个阶段都应单独形成一个或多个可审查提交。

---

## 六、阶段 0：基线保护

### 6.1 开工前检查

执行：

```powershell
git status --short
.\gradlew.bat cleanTest test
```

记录现有用户修改，不得覆盖。当前已知工作树可能包含：

- `CHANGELOG.md`
- `src/main/java/finance/client/FinanceScreen.java`
- `src/main/java/finance/client/MarketOverviewScreen.java`
- `logs/` 下运行日志

实施窗口必须重新检查实际状态，不能假设这些改动仍然相同。

### 6.2 建立设计文档

在 `docs/` 中新增：

- `MINECRAFT_FIRST_DESIGN.md`
- `GAMEPLAY_ARCHITECTURE.md`
- `SAVE_COMPATIBILITY_POLICY.md`

文档明确：金融内核保留、世界入口优先、服务端权威、旧存档兼容。

### 6.3 建立测试基线

记录 304 项测试基线。后续测试数量应只增加，不应无原因减少。

### 6.4 阶段验收

- 未改变运行行为。
- 完整测试通过。
- 文档能让新开发者理解为什么不是删除金融模块。

---

## 七、阶段 1：玩法配置与访问模式

### 7.1 新增玩法配置

在 `FinanceConfig` 中增加独立的 `gameplay` 配置区，不复用已有金融开关：

- `minecraftFirstMode`：默认 `true`。
- `requirePhysicalTerminal`：默认 `true`。
- `enablePortableLedger`：默认 `true`。
- `legacyFullScreenKeybind`：默认 `false`。
- `warehouseCapacityEnabled`：默认 `true`。
- `contractsEnabled`：默认 `true`。
- `playerDrivenCompanyProduction`：默认 `true`。
- `allowLegacyAutomaticCompanyProduction`：默认 `true`，用于兼容。
- `advancedFinanceRequiresTerminal`：默认 `true`。
- `adminConsoleRequiresPermission`：默认 `true`。

注意：这些配置控制入口和玩法方式，不得阻止已有合同到期、订单取消、债权兑付或存档加载。

### 7.2 新增入口枚举

新增 `FinanceTerminalType`：

```java
PORTABLE_LEDGER,
MARKET_TERMINAL,
WAREHOUSE_CONTROLLER,
BANK_COUNTER,
COMPANY_DESK,
SECURITIES_TERMINAL,
BOARDROOM_TABLE,
CENTRAL_BANK_CONSOLE,
LEGACY_FULL_SCREEN
```

新增 `FinanceScreenMode`，区分客户端显示模式：

- `WALLET`
- `MARKET`
- `WAREHOUSE`
- `COMPANY`
- `BANK`
- `ADVANCED`
- `ADMIN`

### 7.3 统一打开请求

扩展或替换 `OpenFinanceGuiPacket`，让服务端根据入口决定允许的模式。不能相信客户端自行声明管理员或高级终端权限。

验证项目：

- 玩家是否正在使用对应菜单或方块。
- 方块是否仍位于原位置。
- 距离是否在允许范围。
- 管理员模式是否拥有 2 级权限。
- 入口类型和请求模式是否匹配。

### 7.4 保留旧入口

旧快捷键不删除：

- 在 `minecraftFirstMode=false` 时保持现状。
- 在默认模式下打开随身账本或提示玩家制作市场终端。
- 管理员可以通过命令或配置打开旧完整界面。

### 7.5 测试

新增纯逻辑测试：

- 普通钱包入口不能打开管理员模式。
- 市场终端只能打开市场相关界面。
- 高级终端可打开完整金融界面。
- 关闭 Minecraft-first 模式后保持旧行为。

---

## 八、阶段 2：简洁主界面与高级界面分流

### 8.1 不直接重写 `FinanceScreen`

保留当前 `FinanceScreen` 作为高级金融界面。优先新增轻量屏幕：

- `WalletScreen`
- `MarketTerminalScreen`
- `WarehouseScreen`
- `CompanyDeskScreen`

如果复用当前 `FinanceMenu` 数据负载过重，应新增更小的菜单/响应包，不要让随身钱包每次同步全部公司、股票、提案和市场历史。

### 8.2 随身钱包界面

显示：

- 可用余额
- 冻结余额
- 总资产的简略值
- 最近交易
- 当前合同数量
- “寻找市场”提示

禁止显示：

- 全量 K 线
- 全市场公司列表
- 管理员按钮
- 银行完整资产负债表
- 期货和复杂治理

### 8.3 市场终端界面

普通视图包含：

- 搜索和商品分类
- 当前中间价、买价、卖价、日涨跌
- 玩家托管数量、附近仓库容量
- 简单买入、卖出、挂单
- 本地收购合同

增加“高级行情”按钮；只有从证券终端或拥有相应解锁时才进入现有 K 线界面。

### 8.4 信息层级

使用 Minecraft 语言而不是专业金融术语：

- “可用金币”优先于“流动资金”
- “已锁定金币”优先于“冻结余额”
- “市场收购价/出售价格”优先于“Bid/Ask”
- “仓库数量”优先于“托管头寸”
- 高级提示中仍可保留专业名词

### 8.5 客户端缓存

沿用现有请求编号防止迟到响应覆盖新状态。新界面也必须区分：

- 未请求
- 加载中
- 响应慢
- 空数据
- 成功
- 无权限

### 8.6 验收

- 新玩家默认不会看到完整超级金融界面。
- 现有高级界面仍可访问且功能未减少。
- 普通界面的网络负载明显小于完整 `FinanceMenu`。

---

## 九、阶段 3：方块、物品和注册骨架

### 9.1 注册体系

当前项目主要注册菜单，需新增标准 Forge DeferredRegister：

- `ModBlocks`
- `ModItems`
- `ModBlockEntities`
- 扩展 `ModMenus`
- 必要时增加创造模式标签页内容

### 9.2 第一批内容

建议先加入：

- `portable_ledger`：随身账本
- `market_terminal`：市场终端
- `warehouse_controller`：仓库控制器
- `company_desk`：公司办公桌
- `securities_terminal`：证券终端
- `bank_counter`：银行柜台
- `central_bank_console`：央行控制台

董事会桌可以稍后加入。

### 9.3 方块交互标准

所有终端方块：

- 服务端验证玩家距离。
- 方块被移除时关闭菜单或拒绝新操作。
- 客户端只负责动画和界面。
- 有明确材质、名称、tooltip 和用途说明。
- 放置后不自动创造资金、商品或账户资产。

### 9.4 配方原则

- 随身账本成本低：书、纸、少量金属。
- 市场终端需要红石、铁和玻璃。
- 仓库控制器需要箱子、铁和红石。
- 证券终端更昂贵，并要求钻石/下界石英等中后期材料。
- 央行控制台不提供普通生存配方，管理员获取。

### 9.5 资源

添加：

- blockstates
- block models
- item models
- `zh_cn.json`
- `en_us.json`
- loot tables
- recipes
- 必要的 tags

不要把所有显示字符串继续硬编码在 Java 中。新内容必须优先使用翻译 key。

---

## 十、阶段 4：仓库控制器和真实物品托管

这是整个 Minecraft 化工作的关键阶段。

### 10.1 核心概念

现有 `CommodityInventoryManager` 保留，并被定义为“市场托管账本”。仓库控制器负责把真实物品存入托管账本，或从托管账本提取为真实物品。

```text
真实 ItemStack
    ↓ 存入并验证
WarehouseService
    ↓
CommodityInventoryManager
    ↓
MarketManager / NpcMarketMaker
```

### 10.2 仓库实体

新增 `WarehouseControllerBlockEntity`，建议字段：

- 仓库 UUID
- 所有者 UUID
- 可选公司 UUID
- 创建时间
- 容量等级
- 权限模式
- 已连接容器位置（如果采用多方块结构）
- 最后审计时间

不要在 BlockEntity 中复制完整商品账本。权威商品数量仍由已有管理器或新的专用世界 SavedData 管理。

### 10.3 容量模型

首版可以使用简单总槽位或总商品单位上限：

- 基础仓库：若干单位
- 升级后增加容量
- 公司仓库可共享
- 超出容量拒绝存入

需要明确旧虚拟库存的兼容策略：

- 旧资产始终可提取。
- 如果数量超过新仓库容量，不得删除；标记为“历史托管超额”。
- 超额状态下允许取出，不允许继续存入，直到回到容量内。
- 市场挂单所需的已托管商品仍可正常结算。

### 10.4 原子存入

存入流程：

1. 验证商品注册和物品 ID。
2. 验证仓库权限和容量。
3. 统计玩家/容器中的真实物品。
4. 预检 `CommodityInventory` 是否可增加。
5. 扣除真实物品。
6. 增加托管账本。
7. 若第 6 步异常失败，立即返还物品；返还失败则掉落并记录严重日志。
8. 写交易/仓库流水并标脏。

### 10.5 原子提取

提取流程：

1. 验证权限、数量和注册物品。
2. 检查托管可用数量，不得提取已用于卖单的冻结部分。
3. 检查玩家背包/目标容器容量；允许配置溢出掉落，但默认应拒绝不完整提取。
4. 扣减托管账本。
5. 生成真实物品。
6. 若生成失败，恢复托管数量。

现有商品卖单目前通过扣除库存达到冻结效果。需要明确区分“总托管量”和“可用托管量”；如现有模型已经把卖单商品移出库存，则提取只需使用剩余数量，不能重复冻结。

### 10.6 自动化

首版不要立刻支持所有管道模组。先支持：

- 玩家手动存取
- 原版漏斗的有限输入/输出
- 方块菜单中的批量存取

后续再定义 Capability 接口，防止自动化造成重复物品。

### 10.7 测试

- 存入成功时真实物品减少且托管增加相同数量。
- 账本溢出时不扣物品。
- 容量不足时不产生部分状态。
- 提取成功严格守恒。
- 背包已满时按策略拒绝或完整掉落。
- 卖单锁定商品不能被提取。
- 旧存档超额库存不会丢失。
- 服务器重启后仓库身份和托管关系一致。

---

## 十一、阶段 5：市场终端 Minecraft 化

### 11.1 终端附近仓库

市场终端进行卖出时，优先使用：

1. 绑定仓库的托管库存；
2. 玩家个人历史托管库存；
3. 可选的“直接从背包存入并出售”快捷流程。

直接出售必须先经过仓库服务的原子存入，不能让市场管理器直接扫描并扣除客户端声称的背包数量。

### 11.2 地区感

首版不必创建多地区价格，但应在表现上强调终端位置：

- 终端显示所在维度和坐标。
- 公告板任务可以要求在指定终端交付。
- 公司仓库可以绑定某个终端。
- 运输合同可以连接两个仓库/终端。

### 11.3 市场反馈

交易成功时提供：

- 终端音效
- 简短粒子效果
- 屏幕消息
- 对应数量和金额
- 任务进度更新

重大成交或短缺可以通过服务器广播，但必须有冷却，避免刷屏。

### 11.4 NPC 市场定位

保留 `NpcMarketMaker`，但在玩家层表现为：

- 商队/系统收购站
- 基础流动性提供者
- 防止无人服务器完全无法交易的兜底

不要向普通玩家展示央行干预的全部内部数字。可以显示“市场收购额度不足”或“库存紧张”。

### 11.5 订单安全

所有旧规则继续生效：

- 价格和数量正数
- 安全乘法
- 防自成交
- 价格—时间优先
- 失败不移动资产
- 取消返还
- 成交只统计实际数量

新增世界入口不能绕过这些规则。

---

## 十二、阶段 6：合同与任务系统

### 12.1 目标

用任务把金融价格与 Minecraft 行为连接起来。任务不应凭空发钱；资金来源必须明确为公司、NPC 市场预算、玩家托管或系统政策额度。

### 12.2 首版合同类型

建议先实现三种：

1. `DELIVERY`：向指定仓库交付指定商品。
2. `PROCUREMENT`：公司或市场发布收购需求。
3. `TRANSPORT`：从源仓库领取封装货物并运到目标仓库。

以后再增加：

- 建设合同
- 加工合同
- 护送合同
- 公司雇佣任务
- 灾害救援任务
- 银行抵押物补充任务

### 12.3 数据模型

建议：

- `FinanceContract`
- `ContractType`
- `ContractStatus`
- `ContractIssuerType`
- `ContractRequirement`
- `ContractReward`
- `PlayerContractProgress`
- `ContractManager`

必要字段：

- 合同 UUID
- 发布者 UUID/系统类型
- 商品 ID 和数量
- 起点/终点维度与位置（可选）
- 奖励金额
- 创建日和截止日
- 可接受人数
- 当前状态
- 已接受玩家
- 操作幂等 key
- 托管账户/托管商品引用

### 12.4 奖励来源

- 公司合同：发布时从公司现金进入合同托管账户。
- 玩家合同：从玩家账户冻结或转入托管。
- NPC 市场合同：从 `NpcMarketMaker.NPC_UUID` 的明确余额支付。
- 政策任务：使用有上限、有记录的系统发行来源，不能调用无来源的 `deposit` 直接造币。

### 12.5 交付结算

交付必须：

1. 验证合同仍有效且玩家已接受。
2. 验证交付地点。
3. 验证真实物品或托管货物。
4. 预检奖励接收方是否可入账。
5. 扣除物品或转移封装货物。
6. 从合同托管向玩家转账。
7. 更新任务状态和统一交易记录。
8. 任一步失败都不能留下部分完成。

### 12.6 运输货物

运输合同可以使用不可拆分的“封装货箱”物品：

- 货箱 NBT 只保存合同 ID 和签名/校验信息，不把客户端 NBT 当作权威数量。
- 服务端保存货物实际内容。
- 丢弃、死亡或烧毁时按合同规则处理。
- 防止复制、重复交付和改 NBT。

首版如风险过高，可以先只做同一仓库的交付合同，不急于实现货箱。

### 12.7 任务生成

合同可来源于：

- 公司原材料低于安全库存
- NPC 市场库存过低
- 某商品价格异常上涨
- 保险事件后的补充需求
- 管理员手动发布

自动生成必须有：

- 每商品/公司冷却
- 最大活动合同数
- 奖励上限
- 去重
- 过期清理
- 服务端性能预算

### 12.8 测试

- 重复 operation key 不能重复领奖。
- 奖励账户不足时合同保留且物品不丢失。
- 玩家背包伪造不能完成任务。
- 过期任务不能结算。
- 保存/加载后任务进度不重复。
- 公司破产时未执行合同按规则退款或取消。

---

## 十三、阶段 7：公司经营世界化

### 13.1 双模式兼容

公司增加经营模式：

- `LEGACY_AUTOMATIC`：保留当前自动生产和自动交易。
- `PLAYER_DRIVEN`：通过公司仓库、任务和设施产生经营结果。
- `HYBRID`：玩家交付为主，低效率自动生产为兜底。

旧公司加载时默认保持 `LEGACY_AUTOMATIC`，避免升级后突然停产。新公司在 Minecraft-first 模式下默认 `PLAYER_DRIVEN` 或 `HYBRID`。

### 13.2 公司办公桌

功能：

- 查看公司现金、库存和近期利润
- 管理成员角色
- 绑定公司仓库
- 发布采购合同
- 设置自动出售比例
- 查看设施等级
- 进入高级董事会界面

### 13.3 成员与权限

当前公司主要绑定 owner。新增成员体系时不要破坏 owner 语义。建议角色：

- OWNER
- MANAGER
- TREASURER
- WAREHOUSE_WORKER
- MEMBER

权限必须服务端验证：

- 使用公司资金
- 存取公司仓库
- 发布合同
- 修改自动出售
- 查看敏感财报
- 发起治理操作

### 13.4 生产设施

首版避免造复杂机器系统，可先用“设施等级 + 真实材料交付”实现：

- 玩家向公司仓库交付原料。
- 每日结算根据仓库实际原料生产商品。
- 生产升级提升转换效率或每日上限。
- 仓储升级提升容量。
- 管理升级降低运营成本或增加合同数量。

现有 `CompanyType.getDailyConsumption()` 和 `getDailyProduction()` 可以继续作为配方基础。

### 13.5 设施升级的真实成本

升级不能只扣金币。应同时要求：

- 公司现金
- 指定建筑材料
- 可选的最低办公室/仓库条件

通过 `CompanyUpgradeRequirementService` 根据公司类型返回材料需求。

### 13.6 财报来源

现有财报继续使用，但收入和成本应能够区分：

- 玩家交付采购成本
- 自动采购成本
- 市场销售收入
- 合同发布费用
- 设施维护成本
- 贷款利息

不要立即修改所有财报结构。先在交易记录和摘要中提供分类，必要时再进行数据版本升级。

### 13.7 破产表现

保留现有破产清算顺序，同时增加世界反馈：

- 公司办公桌显示风险状态。
- 公司设施停止接受新任务。
- 仓库进入只出不进或清算模式。
- 公开公告板显示清算事件。
- 不直接破坏玩家建筑。

### 13.8 测试

- 旧公司升级后继续原模式运行。
- 玩家驱动公司不凭空生成没有原料的商品。
- 材料和现金升级原子结算。
- 非授权成员不能使用公司资产。
- 公司重启后绑定仓库和成员权限保持。

---

## 十四、阶段 8：世界经济反馈

### 14.1 目标

让经济状态在玩家不打开 GUI 时也能被感知。

### 14.2 公告和提示

新增有节制的事件提示：

- 某商品进入严重短缺
- 某公司发布大额采购
- 某市场库存恢复
- 某公司进入破产风险
- 某保险灾害触发救援需求

消息按重要性选择：

- 终端屏幕局部提示
- 附近玩家提示
- 记分板/公告板
- 全服广播（仅重大事件）

### 14.3 可视反馈

- 市场终端根据库存显示绿色/黄色/红色灯。
- 仓库控制器显示容量等级和满载状态。
- 银行柜台在暂停新业务时改变指示灯。
- 公司办公桌显示正常/风险/清算状态。

不要使用每 tick 大范围方块扫描。状态变化时更新 blockstate，或使用低频缓存。

### 14.4 村民和 NPC

可选后续阶段：

- 金融职员村民职业
- 市场经纪人
- 仓库管理员

首版不应依赖复杂实体 AI；终端方块已经足够建立世界感。

### 14.5 经济事件与真实影响

谨慎实现，避免破坏原版世界：

- 价格事件优先生成高价任务，而不是直接修改方块掉落。
- 灾害优先影响公司仓库和合同，而不是随机摧毁玩家建筑。
- 如果影响农作物或资源产出，必须配置可关闭、范围受限并清晰提示。

---

## 十五、阶段 9：银行与高级金融世界入口

### 15.1 银行柜台

普通玩家功能：

- 选择银行
- 活期存款/提款
- 定期存款
- 查看公开利率和简单风险提示
- 公司贷款申请入口

隐藏内容：

- 完整总账分录
- 资产负债表细项
- 同业拆借细节
- 压力测试
- 存款保险处置控制

这些内容放到管理员或高级后台。

### 15.2 证券终端

直接复用现有高级金融页面能力，但只允许从证券终端打开。可以将原 `FinanceScreen` 调整为接受初始分页和可见分页集合。

必须避免在客户端简单隐藏后仍允许伪造数据包。高级操作的服务端验证继续保留。

### 15.3 董事会桌

用于：

- 创建提案
- 投票
- 执行通过的公司行动
- 查看股权结构

世界入口只增强氛围，不改变快照投票权、参与率、执行授权和幂等规则。

### 15.4 央行控制台

- 仅权限等级 2 的管理员使用。
- 提供当前管理员分页。
- 显示模块暂停状态和诊断摘要。
- 高风险操作需要二次确认。

---

## 十六、阶段 10：进度、配方和教程

### 16.1 渐进解锁

建议使用 Advancement 作为主要解锁提示，不一定要硬性封锁全部功能：

1. 第一次获得金币：解锁随身账本教程。
2. 制作市场终端：解锁商品交易教程。
3. 存入第一批商品：解锁仓库和挂单。
4. 完成第一份合同：解锁公司提示。
5. 创建或加入公司：解锁公司办公桌。
6. 公司上市：解锁证券终端。
7. 首次使用银行：解锁贷款和定期存款说明。

### 16.2 游戏内手册

增加一本简短手册或 Patchouli 兼容层（不要强制依赖第三方模组）。至少包含：

- 如何获得和使用账本
- 如何建造市场终端
- 如何存入仓库
- 如何完成收购合同
- 如何创建公司
- 高级金融终端说明

### 16.3 Tooltip

所有新方块和物品 tooltip 说明“它让玩家做什么”，不要堆砌金融定义。

示例：

- 市场终端：“查看收购价格、出售仓库商品并接受市场订单。”
- 证券终端：“访问股票、债券、基金和期货等高级金融市场。”

---

## 十七、阶段 11：持久化设计

### 17.1 新数据根节点

建议在 `EconomySavedData` 下通过独立序列化器保存：

- `GameplayDataSerializer`
- `WarehouseDataSerializer`
- `ContractDataSerializer`

不要继续把大量新字段直接堆入 `EconomySavedData.save/load` 主体。

### 17.2 加载顺序

建议顺序：

1. 重置世界运行时状态。
2. 加载账户、商品定义和金融内核。
3. 加载仓库身份与绑定。
4. 加载合同和任务进度。
5. 校验合同引用的公司、商品、仓库和托管账户。
6. 隔离无效任务，并安全释放可以确认归属的托管资产。
7. 重建只读派生缓存。

### 17.3 世界切换

`EconomySavedData.resetRuntimeState()` 必须新增清理：

- 仓库运行时索引
- 合同管理器
- 玩家进度缓存
- 终端临时会话
- 世界反馈冷却

客户端登出时也应清空新增缓存。

### 17.4 数据上限

为所有新集合设上限：

- 仓库数量
- 活跃合同数量
- 每玩家接受合同数量
- 完成历史数量
- 世界公告历史

解码集合长度时在分配前验证上限。

---

## 十八、阶段 12：安全、原子性和反滥用

### 18.1 统一操作结果

新玩法服务统一返回结果，不允许 GUI 根据本地推测成功：

```java
record GameplayActionResult(
    boolean success,
    String messageKey,
    List<String> arguments,
    boolean refreshMenu
) {}
```

逐步使用翻译 key，避免继续发送大量硬编码中文。

### 18.2 幂等

以下操作必须有 operation key 或服务端唯一事件 ID：

- 合同发布
- 合同接受
- 合同交付
- 仓库批量转移
- 公司升级
- 设施绑定
- 奖励领取

网络重发或玩家双击不能重复结算。

### 18.3 距离和会话

- 每次修改操作都重新检查玩家与终端距离。
- 菜单打开时有效不代表点击时仍有效。
- 跨维度立即失效。
- 方块被破坏或所有权变化后会话失效。

### 18.4 容器并发

仓库、漏斗和玩家菜单可能同时访问物品。所有结算应在服务器线程完成，并对同一仓库使用同步/事务边界，避免读后写竞争。

### 18.5 管理员权限

管理员终端所有操作必须服务端检查 `hasPermissions(2)`，不能只依赖方块不可合成或按钮隐藏。

---

## 十九、阶段 13：性能要求

### 19.1 禁止行为

- 不允许每 tick 扫描所有已加载方块寻找终端或仓库。
- 不允许每次打开钱包同步整个经济世界。
- 不允许每个合同每天遍历所有玩家背包。
- 不允许客户端请求触发无界列表分配。
- 不允许世界反馈每 tick 全服广播。

### 19.2 索引

维护：

- 仓库 UUID → 仓库数据
- 维度和区块 → 已加载终端/仓库
- 公司 UUID → 绑定仓库
- 商品 ID → 活跃采购合同
- 玩家 UUID → 已接受合同

索引为派生状态时应能从权威数据重建。

### 19.3 日结预算

合同生成、公司生产和世界反馈加入 `FinancialCycleService` 时，要遵循现有逐日幂等模型。大量任务可批量限制，避免时间跳跃导致单 tick 执行无界工作。

### 19.4 长期模拟

扩展 `LongRunSimulationService`：

- 模拟仓库存取
- 模拟合同发布和交付
- 模拟公司玩家驱动生产
- 每 30 日保存/加载
- 保持确定性
- 检查物品等价数量、合同托管资金和金融资产守恒

---

## 二十、测试计划

### 20.1 单元测试

新增建议测试类：

- `FinanceAccessServiceTest`
- `WarehouseServiceTest`
- `WarehouseCapacityTest`
- `ContractManagerTest`
- `ContractSettlementTest`
- `CompanyGameplayServiceTest`
- `GameplayPersistenceTest`
- `GameplayNetworkValidationTest`

### 20.2 必测不变量

- 存入：真实物品减少量 = 托管增加量。
- 提取：托管减少量 = 真实物品增加量。
- 合同：发布者减少资金 = 托管增加资金。
- 完成：托管减少资金 = 玩家增加资金。
- 取消：未支付资金完整返回原主。
- 同一 operation key 只能成功一次。
- 任何溢出、容量不足、引用失效都不改变状态。
- 保存/加载前后资产、合同和仓库身份一致。

### 20.3 网络测试

- 过长商品 ID、任务说明、位置字符串被拒绝。
- 过大数量、期限、集合长度被拒绝。
- 非法枚举 ordinal 被拒绝。
- 旧响应不能覆盖新请求。
- 普通终端不能执行管理员操作。
- 玩家离开终端后修改包被拒绝。

### 20.4 GameTest/集成测试

当前 JUnit 很完整，但世界方块交互最终需要 Forge GameTest：

- 放置终端并打开菜单。
- 仓库与漏斗交互。
- 破坏方块后的会话失效。
- 服务器重启后 BlockEntity 绑定恢复。
- 两名玩家同时交付时只有合法数量被接受。

### 20.5 回归测试

每阶段执行：

```powershell
.\gradlew.bat cleanTest test
```

涉及注册、资源或菜单时额外执行：

```powershell
.\gradlew.bat compileJava
```

如果可行，再进行开发客户端和专用服务端手动冒烟测试。

---

## 二十一、建议提交拆分

不要提交成一个巨型变更。参考：

1. `docs: define Minecraft-first gameplay architecture`
2. `feat: add gameplay access modes and config`
3. `feat: add portable ledger screen`
4. `feat: register finance terminal blocks and items`
5. `feat: add market terminal interaction`
6. `feat: add warehouse identity and persistence`
7. `feat: bridge physical items to commodity custody`
8. `test: cover warehouse atomicity and migration`
9. `feat: add delivery and procurement contracts`
10. `feat: connect contracts to market shortages`
11. `feat: add company desk and member permissions`
12. `feat: add player-driven company production`
13. `feat: route advanced finance through securities terminal`
14. `feat: add world economy feedback and advancements`
15. `test: extend long-run gameplay simulation`
16. `docs: update README and migration guide`

每个提交前检查 `git diff --check`，不要把日志、IDE 文件或无关用户改动带入提交。

---

## 二十二、首个可玩里程碑（MVP）

为了防止长期计划迟迟无法游玩，先完成一个小而完整的版本。

### MVP 范围

- 随身账本物品
- 市场终端方块
- 仓库控制器方块
- 简洁钱包界面
- 简洁市场界面
- 手动存入/取出真实商品
- 使用托管商品进行现有市场交易
- 一种交付合同
- 高级金融终端仍可打开旧完整界面

### MVP 不包含

- 复杂多方块仓库
- 村民职业
- 自动化管道全面兼容
- 运输货箱
- 建筑识别
- 重写公司全部生产
- 世界资源生成变化

### MVP 验收场景

1. 新玩家制作账本，查看余额。
2. 玩家挖到铁，来到仓库控制器存入铁锭。
3. 市场终端显示铁的价格和个人托管量。
4. 玩家向 NPC 市场出售部分铁或挂出 P2P 卖单。
5. 资金正确到账，剩余托管数量正确。
6. 玩家接受铁锭交付合同，再次采集并交付。
7. 合同奖励来自明确托管账户，不能重复领取。
8. 玩家制作/使用证券终端，仍能访问现有股票、基金、期货等全部功能。
9. 重启服务器后余额、商品、订单、仓库和合同均保持。

当以上场景稳定后，再进入公司和世界事件阶段。

---

## 二十三、明确不应做的事情

- 不要删除债券、基金、期货、保险等包。
- 不要为了“简洁”清除旧存档中的高级资产。
- 不要让所有高级模块默认停止每日结算；隐藏入口不等于停止履约。
- 不要把真实物品逻辑直接塞进 `MarketManager`。
- 不要让客户端传入奖励金额或公司利润。
- 不要一次性重写 `FinanceScreen`。
- 不要通过全局静态布尔值临时拼凑所有进度和权限。
- 不要每 tick 扫描区块或玩家背包。
- 不要让经济灾害未经配置直接破坏玩家建筑。
- 不要让新系统绕过 `EconomySavedData.markDirty()` 或世界卸载清理。
- 不要用直接 `AccountManager.deposit()` 给任务凭空发钱，除非明确记录为受限政策发行。
- 不要假设单人环境；所有设计按多人专用服务器处理。

---

## 二十四、交给执行窗口的开工指令

执行窗口应按以下顺序开始：

1. 阅读本计划全部内容。
2. 检查仓库根目录是否存在 `AGENTS.md` 或其他开发约定。
3. 执行 `git status --short`，记录并保护现有用户修改。
4. 阅读以下核心文件：
   - `FinanceMod.java`
   - `FinanceConfig.java`
   - `FinancePacketHandler.java`
   - `OpenFinanceGuiPacket.java`
   - `FinanceGuiOpener.java`
   - `FinanceMenu.java`
   - `FinanceScreen.java`
   - `CommodityInventoryManager.java`
   - `MarketManager.java`
   - `EconomySavedData.java`
   - `FinancialCycleService.java`
5. 运行完整测试，确认基线。
6. 只实施“阶段 0 + 阶段 1”，完成后重新测试并汇报具体改动。
7. 未经确认不要直接跳到仓库、任务和公司大规模实现。

第一轮执行应输出：

- 新增/修改文件列表
- 新的访问模式和配置设计
- 对旧快捷键和旧界面的兼容行为
- 测试结果
- 下一阶段建议

---

## 二十五、最终成功标准

当以下条件同时满足时，才算真正“回到 Minecraft”：

- 新玩家不需要理解股票、债券或银行总账，也能自然开始使用模组。
- 玩家主要通过采集、运输、仓储、建设和合作获得经济收益。
- 商品交易与真实物品守恒，不是纯数字凭空产生。
- 公司需要玩家或设施参与，而不只是后台自动增长。
- 市场变化能生成明确的世界机会和任务。
- 高级玩家仍能访问当前完整金融系统。
- 管理员仍能使用诊断、政策和压力测试。
- 旧存档中的每类资产都有兼容路径。
- 所有关键结算继续满足原子性、幂等性和溢出安全。
- 服务端长期运行和世界切换不会泄漏静态状态。
- 普通界面简单、快速、低网络负载；高级界面保留深度。
- 模组的宣传语可以诚实地变成：

> “在 Minecraft 世界中采集、运输、经营公司和建设贸易网络；从简单市场一路发展到完整金融体系。”

---

## 二十六、推荐的第一步结论

第一步不要动金融算法，也不要删除 GUI 分页。先完成：

1. `minecraftFirstMode` 配置；
2. `FinanceTerminalType` / `FinanceScreenMode`；
3. 旧完整界面定义为高级终端模式；
4. 新的随身钱包轻量入口；
5. 服务端验证“从哪里打开什么界面”。

这一步风险最低，却能立刻建立正确架构边界。后续市场终端、仓库、合同和公司世界化都能沿该边界扩展，而无需破坏已经稳定的金融内核。

